import { Suspense } from "react";
import { UpdatePasswordPage } from "@/components/auth/update-password-page";

export default function PasswordRecoveryPage() {
  return <Suspense><UpdatePasswordPage /></Suspense>;
}
